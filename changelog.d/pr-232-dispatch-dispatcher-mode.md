## Added
- `dispatch` CLI subcommand: agent-callable one-step worktree-isolated orchestration spawn
- Dispatcher mode: built-in seeded mode that teaches agents to decompose work and dispatch isolated units
